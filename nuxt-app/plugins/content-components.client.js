// Plugin, um die Client-seitige Hydration für Komponenten in Markdown-Dateien zu unterstützen
export default defineNuxtPlugin(() => {
  // Stellt sicher, dass der client-seitige Rendering-Prozess korrekt funktioniert
  // Diese leere Plugin-Definition triggert das korrekte Hydration-Verhalten für Komponenten in Markdown
})